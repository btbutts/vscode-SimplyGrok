{
  "name": "vscode-grok",
  "displayName": "Simply Grok for VSCode",
  "description": "Send source files to Grok and a question.",
  "version": "1.2.3",
  "publisher": "ErikKralj",
  "repository": {
    "type": "git",
    "url": "https://github.com/eriktodx/vscode-grok.git"
  },
  "icon": "resources/vscode-grok.png",
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Programming Languages",
    "Machine Learning",
    "AI",
    "Chat"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "configuration": {
      "title": "Simply Grok for VSCode Settings",
      "properties": {
        "vscodeGrok.apiKey": {
          "type": "string",
          "default": "",
          "description": "xAI API key for external service integration.",
          "scope": "secret"
        },
        "vscodeGrok.model": {
          "type": "string",
          "default": "grok-4",
          "enum": [
            "grok-code-fast",
            "grok-4",
            "grok-4-1-fast-reasoning",
            "grok-4.20-reasoning",
            "grok-3",
            "grok-3-mini"
          ],
          "description": "The default xAI model to use with Grok."
        },
        "vscodeGrok.outputMethod": {
          "type": "string",
          "enum": [
            "tab",
            "outputChannel"
          ],
          "default": "tab",
          "description": "Where to display Grok responses:\n - 'tab' for a new editor tab\n - 'outputChannel' for the Output panel."
        },
        "vscodeGrok.showPreview": {
          "type": "string",
          "enum": [
            "always",
            "workspace-only",
            "never"
          ],
          "default": "workspace-only",
          "description": "Controls when a preview of the data being sent to the Grok API is displayed.\n'always' shows a preview for all requests, 'workspace-only' shows a preview only for requests\ninvolving the entire workspace (useful for reviewing large file sets), and 'never'\ndisables the preview entirely. Defaults to 'workspace-only' to balance usability and\nefficiency, as workspace requests often involve more data."
        },
        "vscodeGrok.excludeFiles": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "**/.vscode/**",
            ".vscodeignore",
            "**/node_modules/**",
            "**/.git/**",
            "**/dist/**",
            "**/out/**",
            "**/*.min.*",
            ".gitignore",
            ".dockerignore",
            "package-lock.json",
            "README.md",
            "LICENSE",
            "CHANGELOG.md",
            "CONTRIBUTING.md",
            "esbuild.js",
            "eslint.config.js",
            "tsconfig.json",
            ".vscode-test.mjs",
            "vscode-grok-*.vsix",
            "**/resources/lib/**",
            ".prettierignore",
            ".prettierrc",
            ".npmrc",
            "**/.svelte-kit/**"
          ],
          "description": "Exclude files or directories by glob patterns or exact matches from workspace queries.\n(e.g., ['**/.vscode/**', '**/node_modules/**', '.gitignore'])"
        },
        "vscodeGrok.includeFiles": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Include files or directories by glob patterns or exact matches from workspace queries.\nThis setting takes precedence over 'vscodeGrok.excludeFiles'.\n(e.g., ['**/src/**', '**/.vscode/settings.json', '.dockerignore'])"
        },
        "vscodeGrok.enableStatefulSessions": {
          "type": "boolean",
          "default": false,
          "description": "Enable stateful sessions with Grok, allowing context retention across multiple interactions."
        },
        "vscodeGrok.submitWithEditor": {
          "type": "boolean",
          "default": false,
          "description": "Enable submitting questions via a dedicated editor\nwindow instead of the Command Palette Input Box.\n(supports multiline, copy/paste, tab-switching)"
        },
        "vscodeGrok.tokensPerRequest": {
          "type": "integer",
          "default": null,
          "enum": [
            1024,
            2048,
            4096,
            8192,
            16384,
            24576,
            32768,
            49152,
            65536,
            131072,
            262144
          ],
          "description": "The maximum number of tokens that may be sent to Grok in a single request.\nAdjust based on your needs and API model limits.\nIf unset, Grok defaults will apply."
        },
        "vscodeGrok.captureResponseData": {
          "type": "boolean",
          "default": false,
          "description": "Enable capturing of raw response data from the Grok API for debugging purposes.\nWhen enabled, raw JSON responses will be saved to the extension's global storage\ndirectory with filenames based on the response ID."
        }
      }
    },
    "commands": [
      {
        "command": "vscode-grok.askGrokWorkspace",
        "title": "Ask Grok: Workspace",
        "description": "Ask Grok a question about the entire workspace."
      },
      {
        "command": "vscode-grok.askGrokTab",
        "title": "Ask Grok: Current Tab",
        "description": "Ask Grok a question about the contents of the currently active editor tab."
      },
      {
        "command": "vscode-grok.askGrokFunction",
        "title": "Ask Grok: Function Under Cursor",
        "description": "Ask Grok a question about the function at the cursor position."
      },
      {
        "command": "vscode-grok.askGrokSelection",
        "title": "Ask Grok: Selected Text",
        "description": "Ask Grok a question about currently selected text in active tab."
      },
      {
        "command": "vscode-grok.toggleStatefulAPI",
        "title": "Set Grok: Enable Stateful Sessions",
        "description": "Enable stateful sessions with Grok, allowing context retention across multiple interactions."
      },
      {
        "command": "vscode-grok.clearStatefulSession",
        "title": "Ask Grok: Clear Stateful Session",
        "description": "Clear the current stateful session context stored in the extension."
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js && npm run fetch-dicts",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production && npm run fetch-dicts",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vscode-test",
    "fetch-dicts": "node fetch-dicts.mjs"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.10.4",
    "@types/vscode": "^1.107.0",
    "@typescript-eslint/eslint-plugin": "^8.22.0",
    "@typescript-eslint/parser": "^8.22.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.4.1",
    "esbuild": "^0.27.2",
    "eslint": "^9.19.0",
    "fs-extra": "^11.3.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "ignore": "^7.0.5",
    "minimatch": "^10.1.1",
    "typo-js": "^1.3.1"
  }
}
